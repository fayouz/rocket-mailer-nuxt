// Local playground: `npm run dev` (port 3002). The origin http://localhost:3002 must be allowed on the application.
export default defineNuxtConfig({
  extends: ['..'],
  compatibilityDate: '2026-09-01',
  devtools: { enabled: false },
})
